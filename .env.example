# .env.example — Este archivo se sube a GitHub (es solo un ejemplo sin valores reales)
# Copialo como ".env" y ponele tu API key real

# API key de plant.id — la obtenés en https://web.plant.id/api-access/
PLANT_ID_API_KEY=tu_api_key_aqui
